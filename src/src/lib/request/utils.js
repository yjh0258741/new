export const getQueryUin = () => {
  // const { login } = store.getState();
  // let uin = DEFAULT_UIN;

  // if (login.isLogin && login.userInfo) {
  //   if (process.env.NODE_ENV === 'production') {
  //     uin = login.userInfo.UserID;
  //   }
  // }

  // return uin;
};

